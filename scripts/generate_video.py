import os\nimport gtts\nimport cv2\nimport numpy as np\n\ndef read_chinese_text(file_path):\n    with open(file_path, 'r', encoding='utf-8') as file:\n        text = file.read()\n    return text\n\ndef generate_speech(text, output_path):\n    tts = gtts.gTTS(text, lang='zh')\n    tts.save(output_path)\n\ndef create_image_with_text(text, output_path):\n    # Create a blank image\n    img = np.zeros((720, 1280, 3), dtype=np.uint8)\n    cv2.putText(img, text, (50, 360), cv2.FONT_HERSHEY_SIMPLEX, 2, (255, 255, 255), 3)\n    cv2.imwrite(output_path, img)\n\ndef combine_images_to_video(image_folder, video_output_path):\n    images = [img for img in os.listdir(image_folder) if img.endswith('.png')]\n    images.sort()  # Make sure the images are sorted\n    frame = cv2.imread(os.path.join(image_folder, images[0]))\n    height, width, layers = frame.shape\n    video = cv2.VideoWriter(video_output_path, cv2.VideoWriter_fourcc(*'mp4v'), 1, (width, height))\n\n    for image in images:\n        video.write(cv2.imread(os.path.join(image_folder, image)))\n\n    cv2.destroyAllWindows()\n    video.release()\n\ndef main():\n    text_file = 'content/scripts.txt'\n    speech_file = 'output/speech.mp3'\n    image_folder = 'output/images/'\n    video_output = 'output/video.mp4'\n\n    text = read_chinese_text(text_file)\n    generate_speech(text, speech_file)\n    create_image_with_text(text, os.path.join(image_folder, 'text_image.png'))\n    combine_images_to_video(image_folder, video_output)\n\nif __name__ == '__main__':\n    main()